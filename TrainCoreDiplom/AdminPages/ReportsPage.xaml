<Page x:Class="TrainCoreDiplom.AdminPages.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:System="clr-namespace:System;assembly=mscorlib"
      Title="Отчеты и аналитика"
      MaxHeight="1080" MaxWidth="1920"
      MinHeight="700" MinWidth="1000">

    <Page.Resources>
        <Style x:Key="ReportCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0"
        Background="White"
        CornerRadius="12"
        Padding="20"
        Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                   Text="Отчеты и аналитика"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#1E88E5"/>

                <!-- Кнопка назад -->
                <Button Grid.Column="1"
                Content="← Назад"
                Style="{StaticResource SecondaryButtonStyle}"
                Height="40"
                Width="100"
                Click="BackButton_Click"/>
            </Grid>
        </Border>

        <!-- Период отчета -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="12"
                Padding="20"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="Период:"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <DatePicker Grid.Column="1" 
                            x:Name="StartDatePicker"
                            />

                <TextBlock Grid.Column="2" 
                           Text="—"
                           FontSize="16"
                           VerticalAlignment="Center"
                           Margin="10,0"/>

                <DatePicker Grid.Column="3" 
                            x:Name="EndDatePicker"
                            />

                <ComboBox Grid.Column="4" 
                          x:Name="ReportTypeComboBox"
                          Style="{StaticResource ModernComboBoxStyle}"
                          Width="200"
                          Margin="20,0,0,0"
                          HorizontalAlignment="Left">
                    <ComboBoxItem Content="Продажи по дням" IsSelected="True"/>
                    <ComboBoxItem Content="Продажи по маршрутам"/>
                    <ComboBoxItem Content="Продажи по типам вагонов"/>
                    <ComboBoxItem Content="Заполняемость поездов"/>
                    <ComboBoxItem Content="Выручка по направлениям"/>
                </ComboBox>

                <Button Grid.Column="5"
                        Content="Сформировать отчет"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Height="45"
                        Width="180"
                        Click="GenerateReport_Click"/>
            </Grid>
        </Border>

        <!-- Табличные данные -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="12"
                Padding="20">
            <DataGrid x:Name="ReportDataGrid"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      RowHeight="35"
                      Background="Transparent"
                      BorderThickness="0"/>
        </Border>
    </Grid>
</Page>