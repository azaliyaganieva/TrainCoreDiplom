<Window x:Class="TrainCoreDiplom.AdminWindows.SeasonalPriceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сезонный коэффициент"
        Height="460" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Настройка сезонного коэффициента"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="#1E88E5"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Название сезона" Margin="0,0,0,5"/>
            <TextBox x:Name="SeasonTextBox" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>

            <TextBlock Text="Коэффициент" Margin="0,0,0,5"/>
            <TextBox x:Name="CoeffTextBox" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15" Text="1.0"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Дата начала" Margin="0,0,5,5"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Дата окончания" Margin="5,0,0,5"/>

                <DatePicker Grid.Row="1" Grid.Column="0" x:Name="StartDatePicker" 
                           Style="{StaticResource ModernDatePickerStyle}" Margin="0,0,5,0"/>
                <DatePicker Grid.Row="1" Grid.Column="2" x:Name="EndDatePicker" 
                           Style="{StaticResource ModernDatePickerStyle}" Margin="5,0,0,0"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Отмена" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Width="100" Height="40"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button Content="Сохранить" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="100" Height="40"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>