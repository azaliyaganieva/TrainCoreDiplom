<Window x:Class="TrainCoreDiplom.AdminWindows.SeatEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование места"
        Height="526" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#1E88E5"
                CornerRadius="8"
                Padding="15"
                Margin="0,0,0,20">
            <TextBlock x:Name="TitleText"
                       Text="Данные места"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Padding="20">
            <StackPanel>
                <TextBlock Text="Номер места" 
                           FontWeight="SemiBold" 
                           Foreground="#1E88E5"
                           Margin="0,0,0,5"/>
                <TextBox x:Name="NumberTextBox" 
                         Style="{StaticResource ModernTextBoxStyle}" 
                         Margin="0,0,0,15"/>

                <TextBlock Text="Тип места" 
                           FontWeight="SemiBold" 
                           Foreground="#1E88E5"
                           Margin="0,0,0,5"/>
                <ComboBox x:Name="TypeComboBox" 
                          Style="{StaticResource ModernComboBoxStyle}" 
                          Margin="0,0,0,15">
                    <ComboBoxItem Content="Стандартное" IsSelected="True"/>
                    <ComboBoxItem Content="Боковое"/>
                    <ComboBoxItem Content="У окна"/>
                    <ComboBoxItem Content="У прохода"/>
                </ComboBox>

                <TextBlock Text="Цена" 
                           FontWeight="SemiBold" 
                           Foreground="#1E88E5"
                           Margin="0,0,0,5"/>
                <TextBox x:Name="PriceTextBox" 
                         Style="{StaticResource ModernTextBoxStyle}" 
                         Margin="0,0,0,15"/>

                <CheckBox x:Name="IsAvailableCheckBox" 
                          Content="Место доступно для бронирования"
                          IsChecked="True"
                          Margin="0,5"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">

            <Button Content="Отмена" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Width="100" Height="40"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>

            <Button Content="Сохранить" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="100" Height="40"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>